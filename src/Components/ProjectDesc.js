function ProjectDesc(){
    return (
        <div className="about-project" id="main-about-project">
            <h1>About the Project</h1>
            <p>The project aims to make a UI with the help of JavaScript library react.js that accepts elements choosen by the user to create a stack that can also shrink and grow dynamically based of the user response</p>
            <p>Internally, a react state is maintained in the form of an array that gets updated as the used clicks different elements of the UI</p>
        </div>
    );
}

export default ProjectDesc;